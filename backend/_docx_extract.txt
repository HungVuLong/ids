Spring Boot 3.2Boilerplate — IDS BackendAI-Powered Network Intrusion Detection SystemJava 17 + Spring Security 6 + JWT + WebSocket + WebClient + PostgreSQLStackSpring Boot 3.2.3, Java 17, PostgreSQL 15AuthSpring Security 6 + JWT (jjwt 0.12.3)Real-timeWebSocket STOMP + SimpMessagingTemplateML CallWebClient reactive → Python Flask :5000DB MigrationFlyway — V1__init_schema.sqlStatusConfig + Entities + Repos + Security = DONE. Services + Controllers = TODO (Copilot)Hướng Dẫn Sử Dụng BoilerplateToàn bộ code đã sẵn sàng chạy. Copy từng file vào đúng package path, cấu hình application.yml, chạy mvn spring-boot:run. Phần còn TODO là business logic — dùng Copilot comment-driven để complete.Cấu Trúc Packageproject_structure.txtsrc/main/java/com/ids/├── IdsApplication.java├── config/│   ├── SecurityConfig.java       ✅ DONE│   ├── WebSocketConfig.java      ✅ DONE│   └── WebClientConfig.java      ✅ DONE├── security/│   ├── JwtUtil.java              ✅ DONE│   ├── JwtAuthFilter.java        ✅ DONE│   └── UserDetailsServiceImpl.java ✅ DONE├── entity/│   ├── User.java                 ✅ DONE│   ├── Packet.java               ✅ DONE│   ├── Alert.java                ✅ DONE│   └── Report.java               ✅ DONE├── repository/│   ├── UserRepository.java       ✅ DONE│   ├── PacketRepository.java     ✅ DONE│   ├── AlertRepository.java      ✅ DONE│   └── ReportRepository.java     ✅ DONE├── exception/│   ├── ResourceNotFoundException.java  ✅ DONE│   ├── DuplicateResourceException.java ✅ DONE│   └── GlobalExceptionHandler.java     ✅ DONE├── dto/ (request/ response/ ml/)  📝 TODO: dùng Copilot generate├── service/                        📝 TODO: dùng Copilot generate├── controller/                     📝 TODO: dùng Copilot generate└── websocket/                      📝 TODO: dùng Copilot generateQuick Startquickstart.sh# 1. Tạo database PostgreSQLpsql -U postgres -c "CREATE DATABASE ids_db;"psql -U postgres -c "CREATE USER ids_user WITH PASSWORD 'yourpass';"psql -U postgres -c "GRANT ALL PRIVILEGES ON DATABASE ids_db TO ids_user;" # 2. Set environment variablesexport DB_PASSWORD=yourpassexport JWT_SECRET=your-super-secret-key-min-32-characters!!export ML_SERVICE_URL=http://localhost:5000 # 3. Run Spring Bootcd backend/mvn spring-boot:run # 4. Test health endpointcurl http://localhost:8080/actuator/health# Expected: {"status":"UP"} # 5. Register first usercurl -X POST http://localhost:8080/api/auth/register \  -H "Content-Type: application/json" \  -d '{"username":"admin","email":"admin@ids.local","password":"Admin@123"}'Boilerplate Code — File by Filepom.xml — Maven DependenciesSpring Boot 3.2.3, Java 17. Tất cả dependencies đã pin version. Copy nguyên vào root project.pom.xml — Maven Dependencies<?xml version="1.0" encoding="UTF-8"?><project xmlns="http://maven.apache.org/POM/4.0.0" ...>  <parent>    <groupId>org.springframework.boot</groupId>    <artifactId>spring-boot-starter-parent</artifactId>    <version>3.2.3</version>  </parent>  <groupId>com.ids</groupId>  <artifactId>ids-backend</artifactId>  <version>1.0.0</version>  <properties>    <java.version>17</java.version>    <jjwt.version>0.12.3</jjwt.version>  </properties>  <dependencies>    <!-- Web, Security, JPA, WebSocket, WebFlux, Validation, Actuator -->    <dependency>spring-boot-starter-web</dependency>    <dependency>spring-boot-starter-security</dependency>    <dependency>spring-boot-starter-data-jpa</dependency>    <dependency>spring-boot-starter-websocket</dependency>    <dependency>spring-boot-starter-webflux</dependency>    <dependency>spring-boot-starter-validation</dependency>    <dependency>spring-boot-starter-actuator</dependency>    <!-- DB: PostgreSQL + Flyway -->    <dependency>postgresql (runtime)</dependency>    <dependency>flyway-core</dependency>    <dependency>flyway-database-postgresql</dependency>    <!-- JWT: jjwt-api + jjwt-impl (runtime) + jjwt-jackson (runtime) -->    <dependency>jjwt-api 0.12.3</dependency>    <!-- Lombok (optional) -->    <dependency>lombok</dependency>    <!-- Test -->    <dependency>spring-boot-starter-test (test)</dependency>  </dependencies></project> // NOTE: Xem file pom.xml đầy đủ trong zip boilerplate hoặc// dùng ChatGPT prompt: "Generate complete pom.xml for Spring Boot 3.2// with: web, security, jpa, websocket, webflux, validation, actuator,// postgresql, flyway, jjwt 0.12.3, lombok"application.yml — ConfigurationTất cả sensitive values đều dùng env vars. KHÔNG hardcode password vào file này.application.yml — Configurationspring:  datasource:    url: ${DB_URL:jdbc:postgresql://localhost:5432/ids_db}    username: ${DB_USERNAME:ids_user}    password: ${DB_PASSWORD}    driver-class-name: org.postgresql.Driver  jpa:    hibernate:      ddl-auto: validate    show-sql: true    properties:      hibernate:        dialect: org.hibernate.dialect.PostgreSQLDialect  flyway:    enabled: true    locations: classpath:db/migration    baseline-on-migrate: true server:  port: ${SERVER_PORT:8080} jwt:  secret: ${JWT_SECRET:dev-secret-32-chars-minimum!!}  expiration: ${JWT_EXPIRATION:86400000} ml:  service:    url: ${ML_SERVICE_URL:http://localhost:5000} management:  endpoints:    web:      exposure:        include: health,infoV1__init_schema.sql — Flyway MigrationPostgreSQL syntax. Flyway auto-runs khi app start. File đặt tại: src/main/resources/db/migration/V1__init_schema.sql — Flyway MigrationCREATE TABLE IF NOT EXISTS users (    id         BIGSERIAL    PRIMARY KEY,    username   VARCHAR(50)  UNIQUE NOT NULL,    email      VARCHAR(100) UNIQUE NOT NULL,    password   VARCHAR(255) NOT NULL,    role       VARCHAR(20)  NOT NULL DEFAULT 'VIEWER',    created_at TIMESTAMP    NOT NULL DEFAULT NOW(),    updated_at TIMESTAMP    NOT NULL DEFAULT NOW());CREATE TABLE IF NOT EXISTS packets (    id             BIGSERIAL PRIMARY KEY,    src_ip         VARCHAR(45),    dst_ip         VARCHAR(45),    src_port       INTEGER,    dst_port       INTEGER,    protocol       VARCHAR(10),    duration       DOUBLE PRECISION DEFAULT 0,    land           INTEGER DEFAULT 0,    wrong_fragment INTEGER DEFAULT 0,    urgent         INTEGER DEFAULT 0,    label          VARCHAR(20) NOT NULL DEFAULT 'normal',    attack_type    VARCHAR(30),    confidence     DOUBLE PRECISION,    captured_at    TIMESTAMP NOT NULL DEFAULT NOW(),    created_at     TIMESTAMP NOT NULL DEFAULT NOW());CREATE TABLE IF NOT EXISTS alerts (    id          BIGSERIAL PRIMARY KEY,    packet_id   BIGINT REFERENCES packets(id) ON DELETE SET NULL,    attack_type VARCHAR(30)  NOT NULL,    severity    VARCHAR(20)  NOT NULL DEFAULT 'MEDIUM',    status      VARCHAR(20)  NOT NULL DEFAULT 'OPEN',    message     TEXT,    created_at  TIMESTAMP    NOT NULL DEFAULT NOW(),    resolved_at TIMESTAMP,    resolved_by BIGINT REFERENCES users(id) ON DELETE SET NULL);CREATE TABLE IF NOT EXISTS reports (    id            BIGSERIAL PRIMARY KEY,    title         VARCHAR(200) NOT NULL,    generated_by  BIGINT REFERENCES users(id) ON DELETE SET NULL,    from_date     DATE NOT NULL,    to_date       DATE NOT NULL,    total_packets BIGINT NOT NULL DEFAULT 0,    total_alerts  BIGINT NOT NULL DEFAULT 0,    generated_at  TIMESTAMP NOT NULL DEFAULT NOW());-- Performance indexesCREATE INDEX IF NOT EXISTS idx_packets_label       ON packets(label);CREATE INDEX IF NOT EXISTS idx_packets_captured_at ON packets(captured_at);CREATE INDEX IF NOT EXISTS idx_alerts_status       ON alerts(status);CREATE INDEX IF NOT EXISTS idx_alerts_attack_type  ON alerts(attack_type);CREATE INDEX IF NOT EXISTS idx_alerts_created_at   ON alerts(created_at);SecurityConfig.java — Spring Security 6Stateless JWT. Permit /api/auth/** và /ws/**. Dùng @PreAuthorize('hasRole(ADMIN)') trên controller methods.SecurityConfig.java — Spring Security 6@Configuration @EnableWebSecurity @EnableMethodSecurity@RequiredArgsConstructorpublic class SecurityConfig {    private final JwtAuthFilter jwtAuthFilter;    private final UserDetailsService userDetailsService;     @Bean    public SecurityFilterChain securityFilterChain(HttpSecurity http) throws Exception {        return http            .csrf(AbstractHttpConfigurer::disable)            .sessionManagement(s -> s.sessionCreationPolicy(STATELESS))            .authorizeHttpRequests(auth -> auth                .requestMatchers("/api/auth/**").permitAll()                .requestMatchers("/actuator/health").permitAll()                .requestMatchers("/ws/**").permitAll()                .anyRequest().authenticated()            )            .authenticationProvider(authenticationProvider())            .addFilterBefore(jwtAuthFilter,                UsernamePasswordAuthenticationFilter.class)            .build();    }    @Bean PasswordEncoder passwordEncoder() { return new BCryptPasswordEncoder(); }    @Bean AuthenticationProvider authenticationProvider() { ... }    @Bean AuthenticationManager authenticationManager(...) { ... }}WebSocketConfig.java + WebClientConfig.javaWebSocket: STOMP broker /topic, endpoint /ws với SockJS. WebClient: timeout 3s connect / 5s response.WebSocketConfig.java + WebClientConfig.java// WebSocketConfig.java@Configuration @EnableWebSocketMessageBrokerpublic class WebSocketConfig implements WebSocketMessageBrokerConfigurer {    @Override    public void configureMessageBroker(MessageBrokerRegistry r) {        r.enableSimpleBroker("/topic");        r.setApplicationDestinationPrefixes("/app");    }    @Override    public void registerStompEndpoints(StompEndpointRegistry r) {        r.addEndpoint("/ws")         .setAllowedOriginPatterns("*")         .withSockJS();    }} // WebClientConfig.java@Configurationpublic class WebClientConfig {    @Value("${ml.service.url}") private String mlServiceUrl;     @Bean    public WebClient mlWebClient() {        HttpClient httpClient = HttpClient.create()            .option(ChannelOption.CONNECT_TIMEOUT_MILLIS, 3000)            .responseTimeout(Duration.ofSeconds(5));        return WebClient.builder()            .baseUrl(mlServiceUrl)            .clientConnector(new ReactorClientHttpConnector(httpClient))            .defaultHeader(CONTENT_TYPE, APPLICATION_JSON_VALUE)            .build();    }}JwtUtil.java + JwtAuthFilter.javajjwt 0.12.3 API. generateToken() → sign với HS256. JwtAuthFilter extends OncePerRequestFilter, catch mọi exception để không crash request.JwtUtil.java + JwtAuthFilter.java// JwtUtil.java — key methodspublic String generateToken(UserDetails user) {    return Jwts.builder()        .subject(user.getUsername())        .issuedAt(new Date())        .expiration(new Date(System.currentTimeMillis() + expiration))        .signWith(getSigningKey())        .compact();}public boolean validateToken(String token, UserDetails user) {    return extractUsername(token).equals(user.getUsername())        && !isTokenExpired(token);}private SecretKey getSigningKey() {    return Keys.hmacShaKeyFor(        Base64.getEncoder().encode(secret.getBytes())    );} // JwtAuthFilter.java — key flow@Overrideprotected void doFilterInternal(...) {    String authHeader = request.getHeader("Authorization");    if (authHeader == null || !authHeader.startsWith("Bearer ")) {        filterChain.doFilter(request, response); return;    }    try {        String token = authHeader.substring(7);        String username = jwtUtil.extractUsername(token);        if (username != null && SecurityContext.getAuth() == null) {            UserDetails ud = userDetailsService.loadByUsername(username);            if (jwtUtil.validateToken(token, ud)) {                // set SecurityContextHolder authentication            }        }    } catch (Exception e) { log.warn("JWT failed: {}", e.getMessage()); }    filterChain.doFilter(request, response);}Entities — User, Packet, Alert, ReportTất cả dùng @Builder + @NoArgsConstructor + @AllArgsConstructor + @Getter @Setter. Flyway quản lý schema — KHÔNG dùng ddl-auto=create.Entities — User, Packet, Alert, Report// User.java — pattern (tương tự cho Packet, Alert, Report)@Entity @Table(name = "users")@Getter @Setter @Builder @NoArgsConstructor @AllArgsConstructorpublic class User {    @Id @GeneratedValue(strategy = IDENTITY)    private Long id;    @Column(nullable=false, unique=true, length=50) String username;    @Column(nullable=false, unique=true, length=100) String email;    @Column(nullable=false) String password;    @Builder.Default String role = "VIEWER";    @CreationTimestamp @Column(updatable=false) LocalDateTime createdAt;    @UpdateTimestamp LocalDateTime updatedAt;} // Alert.java — relationships@ManyToOne(fetch=LAZY) @JoinColumn(name="packet_id")private Packet packet; @ManyToOne(fetch=LAZY) @JoinColumn(name="resolved_by")private User resolvedBy; // Packet.java — ML fieldsprivate String label;       // "normal" | "attack"private String attackType;  // "DoS" | "Probe" | "R2L" | "U2R"private Double confidence;  // 0.0 - 1.0Repositories — Spring Data JPASpring Data tự generate SQL từ method name — không cần @Query cho các query đơn giản. @Query chỉ dùng cho GROUP BY và native queries.Repositories — Spring Data JPA// PacketRepository.javapublic interface PacketRepository extends JpaRepository<Packet, Long> {    Page<Packet> findByLabel(String label, Pageable p);    Page<Packet> findByAttackType(String type, Pageable p);    long countByLabel(String label);    long countByLabelAndCapturedAtBetween(        String label, LocalDateTime from, LocalDateTime to);} // AlertRepository.javapublic interface AlertRepository extends JpaRepository<Alert, Long> {    Page<Alert> findByStatus(String status, Pageable p);    Page<Alert> findByStatusAndAttackType(        String status, String type, Pageable p);    long countByStatus(String status);     @Query("SELECT a.attackType, COUNT(a) FROM Alert a GROUP BY a.attackType")    List<Object[]> countGroupByAttackType();     @Query(value="SELECT DATE(created_at)::text, COUNT(*) FROM alerts " +                 "WHERE created_at >= :from GROUP BY DATE(created_at) ORDER BY 1",           nativeQuery=true)    List<Object[]> getTimelineSince(LocalDateTime from);}GlobalExceptionHandler.javaTất cả exception trả về ErrorResponse JSON đồng nhất. Đặt trong package exception/.GlobalExceptionHandler.java@RestControllerAdvice @Slf4jpublic class GlobalExceptionHandler {     @ExceptionHandler(ResourceNotFoundException.class)    public ResponseEntity<ErrorResponse> handleNotFound(            ResourceNotFoundException ex, HttpServletRequest req) {        log.warn("Resource not found: {}", ex.getMessage());        return ResponseEntity.status(404).body(ErrorResponse.builder()            .status(404).error("Not Found")            .message(ex.getMessage())            .path(req.getRequestURI())            .timestamp(LocalDateTime.now()).build());    }     @ExceptionHandler(MethodArgumentNotValidException.class)    public ResponseEntity<ErrorResponse> handleValidation(            MethodArgumentNotValidException ex, HttpServletRequest req) {        String msg = ex.getBindingResult().getFieldErrors().stream()            .map(e -> e.getField() + ": " + e.getDefaultMessage())            .collect(Collectors.joining(", "));        return ResponseEntity.status(400).body(ErrorResponse.builder()            .status(400).error("Validation Failed")            .message(msg).path(req.getRequestURI())            .timestamp(LocalDateTime.now()).build());    }     @ExceptionHandler(Exception.class)    public ResponseEntity<ErrorResponse> handleGeneral(            Exception ex, HttpServletRequest req) {        log.error("Unhandled exception", ex);        return ResponseEntity.status(500).body(ErrorResponse.builder()            .status(500).error("Internal Server Error")            .message("An unexpected error occurred")            .path(req.getRequestURI())            .timestamp(LocalDateTime.now()).build());    }}Phần Còn TODO — Dùng Copilot Để CompleteCác phần dưới đây chưa có trong boilerplate. Dùng Copilot Prompt Library (file IDS_Copilot_Prompts_Backend.docx) để generate từng file.File / PackageStatusCopilot Prompt IDdto/request/*.javaTODOS2-BE-03, S2-BE-04, S3-BE-01dto/response/*.javaTODOS2-BE-03, S2-BE-04, S3-BE-01dto/ml/MLRequest + MLResponseTODOS3-BE-02service/AuthService + implTODOS2-BE-03service/PacketService + implTODOS2-BE-04service/AlertService + implTODOS3-BE-01service/MLService + implTODOS3-BE-02service/WebSocketService + implTODOS3-BE-03service/DashboardService + implTODOS3-BE-04service/ReportService + implTODOS4-BE-01controller/AuthControllerTODOS2-BE-03controller/PacketControllerTODOS2-BE-04controller/AlertControllerTODOS3-BE-01controller/DashboardControllerTODOS3-BE-04controller/ReportControllerTODOS4-BE-01websocket/AlertWebSocketHandlerTODOS3-BE-03Unit TestsTODOS4-BE-02
